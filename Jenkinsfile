node {

    docker.image("python:3.6.0").inside {
        stages {
            stage('build') {
                steps {
                    sh 'python --version'
                }
            }
        }
    }
}
