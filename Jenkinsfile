node {

    docker.image("python:3.5.1").inside {
        stages {
            stage('build') {
                steps {
                    sh 'python --version'
                }
            }
        }
}
